{
    "$schema": "http://json-schema.org/schema",
    "$id": "CreatePageSchema",
    "title": "Service Setup Options",
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "Page name",
        "x-prompt": "What would you like to name the page?"
      }
    },
    "required": ["name"]
  }